{
    "getBranch": "SELECT * FROM branch WHERE is_active = true",
    "getBranchById": "SELECT * FROM branch WHERE branch_id = $1",
    "addBranch": "INSERT INTO branch (branch_name,branch_address,open_time,close_time,is_active) VALUES ($1, $2, $3, $4, $5) RETURNING *",
    "updateBranch": "UPDATE branch SET branch_name = $1, branch_address = $2, open_time = $3, close_time = $4, is_active = $5 WHERE branch_id = $6 RETURNING *",
    "deleteBranch": "UPDATE branch SET is_active = false WHERE branch_id = $1 RETURNING *"
}